import { Display } from '../types/display';

const displayClasses: Record<Display, string> = {
    block: 'block',
    ib: 'inline-block',
    inline: 'inline',
    flex: 'flex',
    if: 'inline-flex',
    hidden: 'hidden',
};

export { displayClasses };
